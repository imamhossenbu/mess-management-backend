# Database and JWT
DATABASE_URL=
JWT_SECRET=
JWT_EXPIRES_IN=7d
PORT=5001
FRONTEND_URL=http://localhost:3000

# Nodemailer SMTP (required for super-admin-created credentials to be emailed)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
